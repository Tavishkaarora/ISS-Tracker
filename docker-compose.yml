version: '3'
services:
  flask:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - redis
  redis:
    image: "redis:latest"
    volumes:
      - ./data:/data
    command: ["redis-server", "--save", "20", "1", "--loglevel", "warning"]

